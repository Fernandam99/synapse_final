import React from 'react';

// Minimal placeholder Dashboard to ensure the project compiles while we fix the full component.
export default function Dashboard() {
  return (
    <div className="dashboard-root">
      <div style={{ padding: 24 }}>
        <h2>Dashboard (placeholder)</h2>
        <p>Componente temporal: reemplázalo por la versión completa cuando estés listo.</p>
      </div>
    </div>
  );
}
